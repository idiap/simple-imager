diff -urN ncurses-6.0.orig/ncurses/base/MKlib_gen.sh ncurses-6.0/ncurses/base/MKlib_gen.sh
--- ncurses-6.0.orig/ncurses/base/MKlib_gen.sh	2015-08-07 02:48:24.000000000 +0200
+++ ncurses-6.0/ncurses/base/MKlib_gen.sh	2017-01-10 12:08:10.019169634 +0100
@@ -72,7 +72,9 @@
 # appears in gcc 5.0 and (with modification) in 5.1, making it necessary to
 # determine if we are using gcc, and if so, what version because the proposed
 # solution uses a nonstandard option.
-PRG=`echo "$1" | $AWK '{ sub(/^[[:space:]]*/,""); sub(/[[:space:]].*$/, ""); print; }' || exit 0`
+# BUG: http://lists.gnu.org/archive/html/bug-ncurses/2015-08/msg00013.html
+#PRG=`echo "$1" | $AWK '{ sub(/^[[:space:]]*/,""); sub(/[[:space:]].*$/, ""); print; }' || exit 0`
+PRG='gcc'
 FSF=`"$PRG" --version 2>/dev/null || exit 0 | fgrep "Free Software Foundation" | head -n 1`
 ALL=`"$PRG" -dumpversion 2>/dev/null || exit 0`
 ONE=`echo "$ALL" | sed -e 's/\..*$//'`
